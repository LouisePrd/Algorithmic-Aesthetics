function setup() {
  createCanvas(500, 500);
  background(230);
  createGrid();
}

function draw() {}

function createGrid() {
    let gap = 30;
    square(gap, gap, width - 2*gap);
}

function addElements() {
    let gap = 30;
    for (i < gap; i++; i < width - gap) {
        for (j < gap; j++; j < height - gap) {

        }
    }
}
